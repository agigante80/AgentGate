{
  "name": "agentgate-npm-manifest",
  "private": true,
  "description": "Single source of truth for the agent-CLI versions. Dependabot (ai-cli group) updates these; the Dockerfile installs exactly these pins via npm install -g.",
  "dependencies": {
    "@github/copilot": "1.0.65",
    "@openai/codex": "0.142.4",
    "@google/gemini-cli": "0.49.0",
    "@anthropic-ai/claude-code": "2.1.195"
  }
}
